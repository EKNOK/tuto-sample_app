<%= form_for(@micropost) do |f| %>
  <%= render 'shred/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compase new micropost..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
